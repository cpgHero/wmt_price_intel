# Start Here

## First Codex interaction

1. Unpack this handoff into a new Git repository.
2. Tell Codex: **Read `AGENTS.md` and the entire handoff package before doing anything.**
3. Send the contents of `prompts/00_CODEX_MASTER_PLANNING_PROMPT.md`.
4. Require a plan-only response. Do not let Codex implement in the same turn.
5. Review the plan, then run the numbered phase prompts one at a time.

## When to attach the large raw category files

Do not make the initial repository unnecessarily large. The package contains compact parser fixtures plus validated outputs. Attach the full August 2026 raw source datasets only when Codex reaches the corresponding full golden-regression phase:

1. Strawberries first.
2. Eggs.
3. Milk.
4. Bananas.

The full location master is included because it is directly required for Phase 2 (Location Master), immediately after the foundation scaffold.
