Implement Phase 0 repository foundation from the approved plan. Create monorepo, pinned dependencies, Python/TypeScript formatting/lint/test configuration, shared JSON Schema validation, local development environment, CI, health endpoints, `.env.example` integration, and database migration framework. Do not implement retailer collection yet. Run all tests and summarize commands/results.
