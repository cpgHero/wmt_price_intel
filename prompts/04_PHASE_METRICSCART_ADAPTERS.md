Implement the shared MetricsCart provider client and V1 retailer adapters for Walmart US, ALDI US, and Amazon Same Day US using `config/retailer-catalog.json` and supplied fixtures. Implement generic nested result-array extraction, raw response object persistence, normalized provider errors, global Postgres-backed 2 RPS/108 RPM permitting, shared 429 cooldown, retry policy, pagination to configured max pages, and per-page credit accounting. Add mocked integration tests; never call production APIs in tests.
