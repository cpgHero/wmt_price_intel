After strawberry is stable, add Product Packs in this order: fresh_shell_eggs, fresh_fluid_milk, fresh_bananas. Use existing generic capabilities wherever possible. Any new generic capability must be category-neutral and tested. Run full golden datasets when attached and reconcile headline benchmarks. Produce an abstraction audit listing every product-specific code path; target is zero in core engine.
