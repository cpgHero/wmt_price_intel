Implement canonical offer normalization, Parquet dataset artifacts, Product Pack loading/validation, the generic classification/matching engine capabilities required by `fresh_strawberries.json`, strict exact-weight comparisons, normalized $/lb comparisons, geographic overlap, and optional 10-mile proximity validation. Do not add strawberry-specific core branches. Run compact fixtures. When full raw datasets are attached, run the full golden regression and reconcile to `fixtures/golden/strawberries/` within declared tolerances.
